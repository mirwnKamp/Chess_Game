package com.mironk.chessgame

class Knight {

    fun getPossibleMoves(start: Int, end: Int): ArrayList<Int> {
        val possibleMoves = arrayListOf<Int>()
        // Implement your logic to calculate possible moves of a knight
        // from the start position to the end position in 1 move.
        return possibleMoves
    }
}